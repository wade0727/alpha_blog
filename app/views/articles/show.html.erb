<div class="well">
  <div class="page-header">
    <h2 align="center">Plant Damage Report: <span class="label-color"><%= @article.title %></span><small> Created By - <%= link_to @article.user.username, user_path(@article.user) %></small></h3>
  </div>
  <div class="row">
    <div class="col-md-4">
      <div class="panel panel-warning">
        <div class="panel-heading">Location</div>
   
          <table class="table">
            <tr>
              <td><b>Node:</b></td>
              <td><%= @article.node %></td>
            </tr>
            <tr>
              <td><b>Street:</b></td>
              <td><%= @article.street %></td>
            </tr>
            <tr>
              <td><b>City:</b></td>
              <td><%= @article.city %></td>
            </tr>
            <tr>
              <td><b>Zip Code:</b></td>
              <td><%= @article.zip %></td>
            </tr>
            <tr>
              <td><b>Subdivision:</b></td>
              <td><%= @article.subdivision %></td>
            </tr>
            <tr>
              <td><b>Key Map:</b></td>
              <td><%= @article.key %></td>
            </tr>
            <tr>
              <td><b>Print Page:</b></td>
              <td><%= @article.print %></td>
            </tr>
          </table>
      </div>
    </div>
    <div class="col-md-4">
      <div class="panel panel-warning">
        <div class="panel-heading">Damage Repair Notations</div>
   
          <table class="table">
            <tr>
              <td><b>Notes:</b></td>
              <td><%= @article.description %></td>
            </tr>
          </table>
      </div>
    </div>
    <div class="col-md-4">
      <div class="panel panel-warning">
        <div class="panel-heading">Plant Details</div>
   
          <table class="table">
            <tr>
              <td><b>Plant Type:</b></td>
              <td><%= @article.plant_type %></td>
            </tr>
          </table>
      </div>
    </div>
    
      <div class="col-md-12">
        <div class="panel panel-info">
          <div class="panel-heading">Damage Repair Notations</div>
     
            <table class="table">
              <tr>
                <td><b>Notes:</b></td>
                <td><%= @article.description %></td>
              </tr>
            </table>
        </div>
      </div>

  </div>
  <div class="pdr-actions">
    <% if logged_in? && (current_user == @article.user || current_user.admin?) %> 
      <%= link_to "Edit this PDR", edit_article_path(@article), class: "btn btn-xs btn-info" %>
      <%= link_to "Delete this PDR", article_path(@article), class: "btn btn-xs btn-danger", method: :delete, data: {confirm: "Are you sure?"} %>
    <% end %>
    <%= link_to "Back PDR Listings", articles_path, class: "btn btn-xs btn-success" %>
  </div>
</div>
